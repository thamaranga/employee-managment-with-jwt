server.port=9899


#Spring security default username is user, default password will be print in the console
#while we deploying the application. If we want to change the default username, password
# we can do it like below. After configuring username and password for accessing our API,
# we need to configure user credentials in postman while calling this API.
#For doing that in postman Authorization -> Basic Auth -> Then enter username, password
#spring.security.user.name=test
#spring.security.user.password=test@123


#=========================================#
#       Database                          #
#=========================================#

spring.datasource.url = jdbc:mysql://localhost:3306/employee
spring.datasource.username = root
spring.datasource.password = root123

#=========================================#
#       JPA/Hibernate                     #
#=========================================#

spring.jpa.show-sql = true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.hibernate.ddl-auto = update
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL8Dialect

